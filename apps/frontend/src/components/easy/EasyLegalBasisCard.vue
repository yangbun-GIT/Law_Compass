<template><article class="basis-card"><p class="eyebrow">{{ text(card.law_name) }}</p><h3>{{ text(card.easy_title) }}</h3><p>{{ text(card.easy_explanation) }}</p><p><strong>이번 사고와 관련된 이유</strong><br />{{ text(card.related_to_this_case) }}</p><div class="chips"><span class="chip">{{ text(card.confidence_label) }}</span><span class="chip">{{ text(card.source_label) }}</span></div></article></template>
<script setup lang="ts">import { sanitizeDisplayText } from "../../utils/displaySanitizer"; defineProps<{ card: any }>(); function text(value: unknown) { return sanitizeDisplayText(value); }</script>
